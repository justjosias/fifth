( This is a comment )
: hello 33 100 108 114 111 119  32  44  111 108 108 101 72   emit emit emit emit emit emit emit emit emit emit emit emit emit  cr ;

: squared ( n -- n**2 ) dup * ;
: cubed ( n -- n**3 ) dup squared * ;

( : min ( n m -- n x   ; )

hello

10 cubed . cr

bye